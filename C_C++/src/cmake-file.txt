cmake_minimum_required(VERSION 3.10)
project(StereoVision)

set(CMAKE_CXX_STANDARD 14)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Add the executable
add_executable(stereo_vision
  main.cpp
  lodepng.cpp
)

# If you have OpenMP installed, you can uncomment the following lines to enable multi-threading
# find_package(OpenMP)
# if(OpenMP_CXX_FOUND)
#     target_link_libraries(stereo_vision PUBLIC OpenMP::OpenMP_CXX)
# endif()
